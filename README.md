# Music Playlist Maker

A beginner command-line Python project for recommending songs by mood, saving songs to JSON, and playing local MP3 files.

## Run With uv

```powershell
uv run python playlist_maker.py
```

## Add Dependencies Later

For example, if you add `pygame` for repeat playback:

```powershell
uv add pygame
```
